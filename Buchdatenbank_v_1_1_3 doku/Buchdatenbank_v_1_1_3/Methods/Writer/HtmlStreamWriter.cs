using System.IO;

namespace Buchdatenbank
{
    internal class HtmlStreamWriter
    {
        //static StreamWriter? writeHtml;

        // Hiermit werden die Informationen erstmal in eine Html-konforme
        // Datei geschrieben. Dies dient nur zu Testzwecken, damit man sieht
        // wie das Ergebnis aussieht.
        /*internal static void WriteToFileHtml(string htmlFile, int fileNumber)
        {
            string path = @".\html\testFileTest_" + fileNumber + ".html";

            StreamWriter writeHtml = new(path);

            writeHtml.WriteLine(htmlFile);
            writeHtml.Close();
        }*/

    
}
}
