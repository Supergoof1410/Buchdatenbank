<Window x:Class="Buchdatenbank.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Buchdatenbank"
             mc:Ignorable="d" 
             Title="Login" Height="200" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
             <Window.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>

                    <Style TargetType="TextBox">
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextWrapping" Value="Wrap"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Top"/>
                        <Setter Property="Height" Value="Auto"/>
                    </Style>

                    <Style TargetType="Button">
                        <Setter Property="Height" Value="Auto"/>
                        <Setter Property="Width" Value="Auto"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
             </Window.Resources>
    <Grid x:Name="LoginUser" ShowGridLines="False">
        <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock 
                    Text="Benutzername:" 
                    Grid.Column="0" Grid.Row="0"
                    VerticalAlignment="Center" HorizontalAlignment="Right"
                    Margin="10,10,10,10"/>
                <TextBlock 
                    Text="Passwort:" 
                    Grid.Column="0" Grid.Row="1"
                    VerticalAlignment="Center" HorizontalAlignment="Right"
                    Margin="10,10,10,10"/>
<!-- 
                <TextBox x:Name="username"
                    Grid.Column="1" Grid.Row="0"
                    Text=""
                    Width="150" Height="Auto"
                    Margin="10,10,10,10"
                    TextChanged="username_TextChanged"/>
                <PasswordBox x:Name="passwd"
                    Grid.Column="1" Grid.Row="1"
                    PasswordChar="*"
                    Width="150" Height="Auto"
                    Margin="10,10,10,10"/>

                <Button x:Name="BtnLogin" Content="Login"
                        Grid.Column="1" Grid.Row="2" 
                        IsEnabled="False" Width="50"
                        HorizontalAlignment="Left" Margin="10,10,0,0"
                        Click="BtnLogin_Click"/>
                <Button x:Name="BtnExit" Content="Exit"
                        Grid.Column="1" Grid.Row="2"
                        Width="50" IsEnabled="True"
                        Margin="70,10,0,0" Click="BtnExit_Click"/> -->
    </Grid>
</Window>
